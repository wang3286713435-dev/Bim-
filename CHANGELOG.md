# Changelog

## v1.7.2

发布时间：2026-05-15
状态：`BIM 日报` P0 优化

已完成：

- 新增“来源内定向召回”，不再只依赖来源首页/最新列表：
  - `BIM建筑网 / buildingSMART`：接入 WordPress 搜索召回
  - `BIMBOX`：接入站内 `?s=` 搜索召回
  - `上海 BIM 推广中心`：接入站内搜索结果页召回
- 每个来源都有独立召回关键词与观察窗口，提升候选池厚度，减少“6 个来源只产出少量文章”的问题。
- 新增 `mergeDailySourceArticles` 合并逻辑：
  - 列表抓取与搜索召回统一去重
  - 保留召回标记与召回关键词，便于后续持续优化
- 新增 AI 审稿式编辑层：
  - 先审阅全部候选资讯
  - 再输出入选顺序、编辑角度与建议跟踪动作
  - 报文不再只是单篇摘要拼接，而是更接近管理层晨报
- `BIM 日报` 主视图新增 `建议跟踪`，继续弱化原始来源附录，进一步强化“成品日报”形态。
- 本地验证结果：
  - 来源数从 `2` 提升到 `4`
  - 入选文章从 `5` 提升到 `8`
  - 候选文章达到 `16`
  - `highlights` 与 `recommendedActions` 已稳定输出
- 新增日报召回测试与编辑层测试，后端测试、前后端构建已通过。

## v1.7.1

发布时间：2026-05-15
状态：`BIM 日报` 管理层可读性升级

已完成：

- 将 `BIM 日报` 从“原始资讯聚合页”升级为“管理层可读日报”形态：
  - 新增 `管理层摘要`
  - 新增 `今日重点 highlights`
  - 新增 `今日新增 / 延续关注` 区分
  - 新增 `候选数 / 入选数 / 来源数` 元数据
- 报道候选池从 `24h + 48h` 扩展为：
  - `24h` 今日新增优先
  - `72h` 最近补充
  - `7d` 延续关注
  以减少“关键词很多但日报正文过少”的问题。
- 单条日报资讯在生成摘要前会优先抓取详情页正文，不再只依赖列表摘录，提高摘要质量与上下文完整度。
- 新增编辑式选稿逻辑：
  - 根据来源权重、时效性、分区类型和关键词命中进行评分
  - 优先保证来源多样性和管理层关注价值
- 前端 `BIM 日报` 页面重构：
  - 首页突出日报正文与管理层摘要
  - 原始资讯列表降级为默认折叠的 `来源附录`
  - 保留关键词筛选、命中 chips 和高亮能力
- 新增日报元数据与编辑逻辑单元测试，日报相关后端测试、构建与前端构建均已通过。

## v1.7.0

发布时间：2026-05-14
状态：`BIM 日报` 独立模块首版

已完成：

- 新增独立的 `BIM 日报` 子系统，与招采机会池、监控词、分析面板并列展示，但数据模型完全隔离：
  - `DailySource`
  - `DailyKeyword`
  - `DailyReport`
  - `DailyArticle`
  - `DailyArticleKeywordHit`
  - `DailyRun`
- 新增 6 个首批日报信源：
  - `ChinaBIM`
  - `BIM建筑网`
  - `BIMBOX`
  - `上海 BIM 推广中心`
  - `Fuzor 官网`
  - `buildingSMART`
- 新增日报专用关键词体系与命中显示，默认内置：
  - `BIM`
  - `数字孪生`
  - `CIM`
  - `Revit`
  - `智慧运维`
  - 以及 `OpenBIM / IFC / ACC / 施工模拟 / 4D / VR / 参数化 / 机电 / 装配式`
- 新增日报专用 API：
  - `GET /api/daily/today`
  - `GET /api/daily/reports`
  - `GET /api/daily/reports/:id`
  - `GET /api/daily/articles`
  - `GET /api/daily/keywords`
  - `GET /api/daily/health`
  - `POST /api/daily/run`
- 新增每日 `09:00` BIM 日报 cron，不影响原有招采日扫任务。
- 前端新增顶级标签 `BIM 日报`，支持：
  - 查看最新日报
  - 查看日报历史
  - 来源筛选
  - 关键词 chips 筛选
  - 标题/摘要命中高亮
  - 查看原始资讯列表与原文链接
- 本地数据库路径在仓库迁移后已改回相对路径口径，避免 `DATABASE_URL` 因目录移动失效。

## v1.6.20

发布时间：2026-05-12
状态：Cloudflare HTTPS 兼容热修

已完成：

- 修复正式域名开启 `FORCE_HTTPS` 后，Cloudflare 透传 HTTPS 请求时仍可能被后端误判为非 HTTPS、从而出现重复 `301` 的问题。
- 后端现在会同时识别：
  - `req.secure`
  - `x-forwarded-proto=https`
  - `cf-visitor: {"scheme":"https"}`
- 这样在 Cloudflare 反代场景下，`https://tender.zhuoyusmart.top` 可以正常登录和访问，而 `http://` 访问仍会被安全跳转到 `https://`。

## v1.6.19

发布时间：2026-05-12
状态：正式域名与 HTTPS 收口

已完成：

- 生产配置切换到正式域名 `https://tender.zhuoyusmart.top`。
- 登录 Cookie 已切到 `Secure` 模式，只通过 HTTPS 传输。
- 新增可选的 `FORCE_HTTPS` 重定向开关；开启后，用户误用 `http://` 访问时会自动跳到 `https://`。
- 这样可以避免“正式域名已上线，但访问明文 HTTP 时登录态无法建立”的体验问题。

## v1.6.18

发布时间：2026-05-12
状态：访问权限保护

已完成：

- 新增前端登录页，未登录时访问域名将先进入登录界面，而不会直接看到招采列表、分析面板和后端运维信息。
- 新增后端会话鉴权：
  - `/api/auth/login`
  - `/api/auth/session`
  - `/api/auth/logout`
- 除登录接口外，`/api` 下其余接口默认都需要登录态，匿名访问会返回 `401`。
- `socket.io` 连接也已接入同一套会话校验，未登录用户无法直接订阅实时公告和通知流。
- 默认账号密码：
  - 用户名：`admin`
  - 密码：`88888888`
- 支持通过环境变量覆盖：
  - `AUTH_USERNAME`
  - `AUTH_PASSWORD`
  - `AUTH_SESSION_SECRET`
  - `AUTH_SESSION_TTL_HOURS`
  - `AUTH_COOKIE_SECURE`

## v1.6.17

发布时间：2026-05-06
状态：调度频率降载

已完成：

- 将默认定时扫描从“每 8 小时一次”调整为“每 24 小时一次”，默认在服务器本地时间 `00:00` 执行。
- 同步更新健康检查返回的调度口径，前端和运维查看时会直接看到“每天自动扫描一次”。
- 调整关键词冷却逻辑中的调度间隔口径，避免调度配置改为 `24h` 后，冷却计算仍沿用旧的 `8h` 批次。
- 补充 `.env.example` 和 README 中的调度配置说明，后续如需再调频，可直接通过环境变量覆盖。

## v1.6.16

发布时间：2026-05-05
状态：关键词冷却窗口修正

已完成：

- 进一步修复关键词冷却的批次对齐问题：冷却锚点现在按“定时扫描批次窗口”取整，而不是按单个关键词在批次中的实际开始秒数/分钟数计算。
- 这次修复避免了同一轮批次中后排关键词因为启动更晚，明明已经过了一个完整冷却周期，却仍被多跳过一个 `8h` 批次的问题。
- 对当前 `0 */8 * * *` 的调度来说，经过该修复后，同一批次进入冷却的关键词会更一致地在下一天对应批次恢复扫描。
- 补充了对应测试，覆盖“startedAt 晚于批次窗口起点时仍应按批次恢复”的场景。

## v1.6.15

发布时间：2026-05-05
状态：关键词冷却调度修正

已完成：

- 修复关键词冷却的一个调度边界问题：冷却窗口现在以“任务开始时间”对齐，而不是以“任务完成时间”对齐。
- 这次修复避免了定时任务因为上轮扫描耗时数分钟，而在下一轮 cron 到点时被多跳过一个周期的情况。
- 对 `0 */8 * * *` 的扫描节奏来说，这意味着“24h 冷却”会更接近真正的“下一天同一批次再尝试”，不会意外拉长到约 `32h`。
- 补充了对应单元测试，覆盖“completedAt 晚于 startedAt 时仍应按 cron 节奏恢复”的场景。

## v1.6.14

发布时间：2026-05-05
状态：观察期效率优化

已完成：

- 新增“连续零产出关键词冷却”机制：定时扫描中，若某关键词连续多轮 `0 saved`，系统会暂时降频，避免继续高频空转。
- 默认策略：
  - 连续 `4` 轮零产出后进入冷却
  - 冷却时长 `24h`
  - 回看窗口 `14` 天
- 手动扫描不受该冷却影响，避免影响临时排查和人工验证。
- 运行配置新增：
  - `TENDER_KEYWORD_COOLDOWN_ZERO_SAVE_THRESHOLD`
  - `TENDER_KEYWORD_COOLDOWN_HOURS`
  - `TENDER_KEYWORD_COOLDOWN_LOOKBACK_DAYS`
- `/api/health` 补充返回关键词冷却配置，方便后续运维核对。
- 补了 `keywordCooldown` 单元测试，并修正 AI fallback 测试基线；服务端测试现已恢复通过。

## v1.6.13

发布时间：2026-04-27
状态：AI 并发稳定性热修

已完成：

- 修复 OpenClaw 本地分析在并发场景下仍会出现 `session file locked` 的问题。
- 后端现在按 `agent + local/remote` 维度为 OpenClaw 建立串行队列：
  - 同一条分析链路优先保成功率，不再因为两条任务同时进入 `main` agent 而互相抢锁
  - `main` 分析与 `bim-tender` 详情补强仍可分 lane 并行
- 这次热修解决了 `v1.6.12` 后仍可能出现的剩余抖动：单次探针成功，但批量重分析或定时扫描并发时仍偶发 fallback。

## v1.6.12

发布时间：2026-04-27
状态：AI 稳定性跟进热修

已完成：

- 修复 OpenClaw 本地运行的新返回结构：当 agent 直接把文本放在顶层 `payloads[].text` 时，后端现在会正确提取正文，而不是把整段运行壳 JSON 当成模型回复。
- 这次修复直接影响两类问题：
  - 列表 AI 判断明明返回成功，但后端仍误判为失败后回退
  - 生产探针能跑通，但真实入库成功率依旧偏低
- 与 `v1.6.11` 叠加后，分析链路现在同时具备：
  - `main + --local` 的轻量稳定入口
  - 独立 session 避免锁冲突
  - 顶层 / 内层 payload 双格式兼容

## v1.6.11

发布时间：2026-04-27
状态：AI 稳定性专项热修

已完成：

- 将 OpenClaw 调用拆成两条链路：
  - 公告相关性判断默认使用 `main` agent
  - 详情字段补强继续使用 `bim-tender` agent
- 公告分析链路默认启用 `--local`，不再先等 gateway 超时再回退本地 runtime，显著降低分析耗时和 AI 回退率。
- 新增可配置项：
  - `OPENCLAW_ANALYSIS_AGENT_ID`
  - `OPENCLAW_ANALYSIS_LOCAL`
  - `OPENCLAW_DETAIL_AGENT_ID`
  - `OPENCLAW_DETAIL_LOCAL`
- 这次修复解决了 `v1.6.10` 之后剩下的主要不稳定来源：分析任务本身并不需要重浏览器 agent，却一直复用更重的 `bim-tender` 路径，导致成功率和响应时间都被拖慢。

## v1.6.10

发布时间：2026-04-27
状态：生产热修

已完成：

- 复盘 OpenClaw 不稳定的直接根因：后端一直复用同一个 agent session，导致 `session file locked`、`live session model switch`、`gateway timeout` 连锁出现，最终把大量分析打回规则回退。
- 后端现在为每一次 AI 调用生成独立的 OpenClaw `session-id`，不再与上一轮分析或详情补强争用同一条 session。
- 这次修复会直接提升两条链路的稳定性：
  - 首页公告相关性判断
  - 详情字段 AI 补强
- 版本 `1.6.9` 已经解决了脏 AI 结果入库；`1.6.10` 进一步解决“为什么 AI 成功率这么不稳定”的主因。

## v1.6.9

发布时间：2026-04-27
状态：生产热修

已完成：

- 修复 OpenClaw 偶发返回“外层运行摘要 JSON”时被误判为成功的问题。现在只接受真正的文本 payload；如果没有 payload，会直接视为失败并进入更安全的回退分支。
- AI 相关性分析新增结果校验：如果返回里缺少 `relevanceReason / summary / relevance`，后端不再把它当作成功结果写库，避免出现 `relevance=0`、`summary=completed`、`reason=null` 这类脏 AI 结果。
- 这次热修和 `v1.6.8` 叠加后，线上 AI 入口已经同时具备：
  - 正确的 OpenClaw agent / config 绑定
  - OpenClaw 不可用时的 OpenRouter 回退能力
  - 不完整 AI 结果的防脏写保护

## v1.6.8

发布时间：2026-04-27
状态：生产热修

已完成：

- 修复生产机 OpenClaw 认证缺失导致的 AI 再次回退问题：当 `AI_PROVIDER=openclaw` 但 OpenClaw agent 因 gateway / auth 不可用时，后端现在会自动回退到已配置的 OpenRouter，而不是整条分析链路直接失败。
- 这条回退同时覆盖：
  - 公告相关性判断
  - 规则兜底前的 AI 摘要生成
  - 文本态的详情字段提取
- 与 `v1.6.7` 叠加后，生产环境已经具备：
  - agent 名称失配时回退到 `main`
  - OpenClaw 本身不可用时回退到 OpenRouter
  - 因此不会再因为单点 AI 运行环境故障让首页大面积只剩规则分析。

## v1.6.7

发布时间：2026-04-27
状态：生产热修

已完成：

- 修复生产机 OpenClaw `agent id` 漂移导致的 AI 全量回退：当 `.env` 中配置的 `OPENCLAW_AGENT_ID` 不存在时，后端会自动回退到 `main` agent，不再因为单个 agent 名称失配让整轮 AI 判断失效。
- 线上同步把 `OPENCLAW_AGENT_ID` 校正为 `main`，解决“列表中 AI 分析消失”背后的根因，而不只是前端展示问题。
- 在 `v1.6.6` 的基础上继续保留：`8h` 定时扫描、OpenClaw 混合输出 JSON 提取、规则回退可视化展示，以及详情字段的二次质量过滤。

## v1.6.6

发布时间：2026-04-27
状态：生产热修

已完成：

- 将定时扫描默认周期从每 `2` 小时调整为每 `8` 小时一次，降低 OpenClaw 分析与详情补强在高峰时段的连续压力；健康检查与启动日志同步改成 `8h` 口径。
- 修复 OpenClaw 输出里混入日志时的 JSON 解析脆弱问题：现在会自动从混合输出里提取有效 JSON，减少 `Unexpected token "[agents/...]"` 这类 AI 回退。
- 机会卡片恢复“AI 判定 / 规则回退”展示。即使当前样本走了规则兜底，也会显示精简后的判断依据，不再看起来像 AI 模块整块消失。
- 强化 AI 详情字段清洗：OpenClaw 浏览器详情补强结果会再次经过单位 / 联系人 / 电话 / 服务范围质量过滤，减少广州等来源被正文噪声污染字段的情况。
- 同步补充监控源 / 历史样本统计口径拆分的本地开发进度，为后续历史样本独立审计做准备。

## v1.6.5

发布时间：2026-04-27
状态：生产热修

已完成：

- 首页投标机会控制区支持折叠：可以收起概览与筛选，避免 Hero 区长期占据过多可视空间。
- 筛选栏的“搜索建议”与“最近搜索”做了去重处理；空查询时不再重复堆出两排相同词条。
- 新增“清空最近搜索”按钮，首页和临时搜索页都可以直接清理历史搜索词。
- 仪表盘“总公告”口径已澄清为当前招采源样本，并额外提示旧版泛热点历史数据仍保留在库内，避免误以为数据丢失。
- `proxy-a-tunnel` 的连续阻断告警已纳入路由优先级判断：即使隧道健康，只要连续命中阈值，也会从“优先使用”降为“自动降级”，先走其他出口或直连。
- 新增生产部署脚本 `scripts/deploy_production.sh`，默认排除 `server/prisma/dev.db`、`.env`、`.secrets` 和构建产物，避免再次把本地 SQLite 数据覆盖到生产机。

## v1.6.4

发布时间：2026-04-27
状态：生产热修

已完成：

- 修复右上角通知面板展开后无法滚动的问题。通知列表现在带有最大高度和纵向滚动，旧通知可以继续向下查看，不会被面板底部截断。
- 通知容器改为固定高度内滚动，避免通知数量变多后把整个浮层撑出可视区域。

## v1.6.3

发布时间：2026-04-27
状态：生产热修

已完成：

- 修复顶部进度条在后台队列运行时长期停在约 `1/5` 位置的问题。现在只有显式刷新/手动扫描时才显示真正的读条；后台队列改成轻量状态提示，不再伪装成卡住的进度。
- 后端热点扫描队列新增陈旧状态自恢复：超过阈值仍未结束的扫描会自动释放队列，并把对应的 `CrawlRun` 标记为失败，避免健康接口长期返回 `running=true`、手动扫描被一直阻塞。
- 通知面板新增点击交互：有 `hotspotId` 的通知可以直接打开项目详情；缺少详情 id 的通知会自动把标题关键词带回首页机会清单做筛选。
- 通知点击后会自动标记已读，并在通知卡片上明确提示“直达详情”或“按关键词筛选主页机会”。

## v1.6.2

发布时间：2026-04-27
状态：生产热修

已完成：

- 修复机会卡片里“AI 辅助判断”对规则回退文案的误展示：`AI 分析超时或失败，使用规则投标分析；规则判断：...` 现在会被识别为 fallback 文案，不再逐条铺满首页。
- 补充 fallback 判定口径，统一覆盖 `未配置 AI 服务 / AI 分析失败 / AI 分析超时或失败 / 规则投标分析 / 规则判断 / 默认分数`。
- 线上复核发现 AI 服务并未整体失效，`AiAnalysisLog` 中仍有 OpenClaw 成功记录；问题核心是历史 fallback 样本较多且前端没有隐藏这类文案。
- 准备对生产库历史 fallback 记录执行重新 AI 分析，尽量把旧的规则回退理由替换为真实 AI 结果。

## v1.6.1

发布时间：2026-04-27
状态：生产热修

已完成：

- 修复顶部“刷新列表与分析数据”进度条偶发卡在约 `3/4` 位置的问题。
- 原因是 `loadPageData` 与 `loadAuxiliaryData` 并发执行时，列表请求先结束并收口进度条，分析请求稍后又把进度推进到 `86%`，但没有对应的 finish，导致进度条看起来像卡住。
- 前端新增统一的 `refreshDashboardData` 刷新编排，并为进度条加入运行批次保护，旧请求的迟到回调不再污染当前进度状态。

## v1.6.0

发布时间：2026-04-27
状态：准备发布到生产

已完成：

- 机会清单新增阶段视图预设、快捷阶段切换和分组筛选卡，支持 `正式公告优先 / 前置信号观察池 / 变更公告复核`。
- 首页机会池视图反馈增强：当前视图、排序口径、阶段标签和摘要字段进一步收紧，首页更偏投标决策而不是长列表堆叠。
- 数据分析页补齐近 7 天机会走势，拆分 `新机会 / 前置信号 / 已截止 / 优先跟进池`，不再只看总量和桶分布。
- 数据分析页新增预算 / 地区 / BIM 类型 / 机会结构的业务化解读，把统计数字翻译成更适合投标团队判断的语言。
- 详情页重组为 `基础信息 / 投标时间与窗口 / 联系人与投标联络 / 服务范围与资质 / 字段来源可信度`，并强化跟进建议、时间轴和可信度说明。
- 浅色主题进一步收口：代理池卡片、阶段标签、可信度标签、详情页头部与机会卡片统一提升对比度和信息密度。
- 修复刷新时版本号短暂闪回 `v1.2.0` 的问题，顶部版本号改为先显示“读取中”，再优先回填真实 `/api/health.version`。
- 针对 13 寸窗口优化主视图分栏策略，减少右侧操作区、优先跟进区和详情页的拥挤、溢出和换行灾难。

## v1.5.4

发布时间：2026-04-26
状态：生产热修，v1.5 正式收口

已完成：

- 修复公告阶段分类误判：`ccgp` 等正式公告不再因为正文包含“采购需求”而被误判成 `采购意向`。
- 公告阶段判定改为优先依据 `公告类型 + 标题`，只在兜底时结合正文信号，避免阶段筛选把正式公告混入“前置信号”。
- 生产机同步 `v1.5.4` 并完成回验：`/api/health` 版本已更新，`公告阶段=pre-signal` 仅保留真正的 `招标计划 / 采购意向` 样本。
- 修复生产库写权限：同步代码后恢复 SQLite 目录写权限，默认关键词初始化与后续入库写入恢复正常。

## v1.5.3

发布时间：2026-04-26
状态：已发布后由 `v1.5.4` 热修覆盖
目标方向：把“多源稳定抓取 + 字段完整性 + 公告阶段分层”正式收口，作为 v1.5 的主线版本。

计划纳入：

- `szygcgpt` 官方详情接口字段正式接通，联系人、电话、项目编号、开标时间、地址、服务范围不再依赖 Firecrawl 才能补齐。
- `guangdong -> szggzy` 回退详情补强稳定化，深圳映射样本可回填真实联系人、电话、项目编号，并清理尾随脏编号。
- `szggzy` / `guangdong` 历史低价值与误提取样本已清洗，合同签订公示、开标情况、无联系人支撑的疑似错误电话不再继续污染机会池。
- 热点 API 增加统一公告阶段口径：输出 `tenderStageCategory / tenderStageLabel / tenderStageBucket / tenderActionable`。
- 前端机会清单、详情页、优先跟进和筛选栏统一改用公告阶段口径，`采购意向 / 招标计划` 明确标记为“前置信号”，不再混入正式可投标项目。
- 本地主线验证通过：7 个启用来源可返回样本，阶段筛选可正确区分 `正式公告 / 资格预审 / 变更补遗 / 前置信号`。

## v1.5.2

发布时间：2026-04-26
状态：生产可用，CEB 详情突破进入会话复用阶段

已完成：

- 新增 CEB 详情安全会话复用机制：支持 `CEB_DETAIL_COOKIE`、`CEB_DETAIL_SESSION_FILE`、`CEB_DETAIL_USER_AGENT`、`CEB_DETAIL_REFERER`、`CEB_DETAIL_EXTRA_HEADERS`。
- CEB 详情接口默认仍关闭；开启 `CEB_DETAIL_FETCH_ENABLED=true` 后，必须提供浏览器会话 Cookie 才会请求真实详情接口，除非显式设置 `CEB_DETAIL_ALLOW_ANONYMOUS=true` 做人工探测。
- CEB 详情来源从匿名硬探测升级为 `ceb-detail-api+session`，成功后复用通用字段质量层清洗单位、预算、联系人、电话、服务范围。
- 新增 `probe:ceb-detail-session` 诊断脚本，用于验证当前 Cookie/会话文件是否可用、是否仍被 WAF、是否能返回结构化详情字段。
- 继续保留列表可信降级：会话缺失、WAF、空响应或错误时，不覆盖列表字段，不拖垮定时扫描。

## v1.5.1

开发启动：2026-04-24
生产更新：2026-04-25
状态：生产可用，继续专项优化
目标方向：中国招标投标公共服务平台专项，提升字段完整性与反爬场景下的稳定降级。

已完成：

- 启动 `cebpubservice` 单源专项，不在本版本扩展新平台。
- 验证 CEB 官方列表页稳定可抓，SPA 详情接口和 Firecrawl scrape 均会触发 WAF/JS challenge。
- CEB 列表字段补强：完整发布时间、公告类型、公告状态、服务范围、开标/截止参考。
- CEB 详情来源升级为 `ceb-list+official-table:v1.5`，前端与飞书可靠性显示“官方列表可信”。
- 新增 `probe:cebpubservice` 诊断脚本，输出样本字段覆盖和分页/反爬状态。
- 新增 `backfill:cebpubservice` 历史字段回填脚本，可 dry-run / apply 修复旧 CEB 记录。
- 修正 v1.5 新源详情增强遗漏：`ccgp`、`ggzyNational`、`cebpubservice` 已纳入原四源详情增强队列。
- 新增 OpenClaw agent 详情字段提取层，低完整度公告会把 URL 和正文交给 `bim-tender` agent 逐条核验详情页。
- Firecrawl 详情抓取增加 WAF/验证码/安全挑战页识别，避免把阻断页当成公告正文。
- 详情增强接口支持按来源补跑，并新增 `enrich:source-details` 脚本，先用于 CEB 单平台专项，后续套到另外两个新平台。
- 修复来源健康误报：关键词空结果不再归类为“疑似被拦截”，24h 失败统计只计请求失败/WAF/熔断等真实运行异常，并优先展示最近非空成功样本。
- 代理池增加主动健康探测：输出隧道可达性、出口 IP、探测延迟、最近探测错误，并在监控日志中区分“主动探测状态”和“真实抓取最近错误”。
- 代理池错误分类细化为 `502 网关异常 / 请求超时 / 隧道断开 / 连接重置 / 目标站阻断 / 认证失败`，并在路由层启用自动降级：健康代理优先，直连次之，异常代理后置，冷却中的代理最后再尝试。
- 新增 `proxyAlerts` 运维告警输出，前端监控日志可直接看到当前代理异常原因和系统的自动降级动作。
- 代理池新增连续异常阈值通知：同一代理连续命中同类异常达到阈值后，后端会输出越线告警，前端代理告警与代理池卡片同步显示“连续异常 X/Y”和触发时间。
- `ggzyNational` 增加来源级查询词分层：当 BIM / 建筑信息模型类查询在全国平台返回空结果时，自动补查 `EPC / 工程总承包 / 全过程工程咨询`，避免把“接口可达但关键词过窄”误判成来源不可用。
- 生产机执行 `cebpubservice` 历史回填：23 条旧样本统一回退到 `ceb-list+official-table:v1.5`，清理 `万元 / 0 / 张三 / 12345678901` 等历史脏值。
- 生产机执行 `cebpubservice` 详情补跑：20 条样本 `changed=0`，确认当前瓶颈仍是详情挑战页，非任务漏跑。
- `ggzyNational` 详情补强口径升级：`0 预算 / 弱单位 / 噪声单位` 也会进入补跑队列；生产机 5 条样本中 4 条已修正单位、服务范围和详情来源。
- `ccgp` 详情链路复核通过：生产机 6 条样本 `queued=0 / changed=0`，当前规则稳定。
- 新增通用字段质量模块 `tenderFieldQuality`，统一清洗单位、预算、联系人、电话、服务范围和详情来源可信度，作为后续复用到其他平台的基础层。
- 来源质量指标改为有效字段口径，并新增电话覆盖率、服务范围覆盖率、详情来源分布，避免脏字段被错误计入质量分。
- 生产机同步 v1.5 字段质量层并完成新源历史样本清洗：34 条样本检查，8 条旧展示残留已规范化，`ggzyNational / ccgp / cebpubservice` 三源脏值计数归零。
- 生产机复核 7 个启用来源均为健康，代理池 `proxy-a / proxy-b` 均为健康优先路由。
- `ggzyNational` 正式复用 `ccgp` 的字段补强策略：列表页命中后自动解析全国平台真实 `b` 详情页，并提取单位、预算/限价、截止/开标、项目编号、联系人、电话、服务范围。
- 字段质量层补强本地座机号与单位尾巴清洗，避免 `85915166` 被误判无效，也避免 `项目已具备招标条件` 混入招标单位字段。
- 生产版本号改为明确递增的 `1.5.x`，不再用 `-dev` 字段；每次生产机更新必须先升级版本号。
- 生产机 v1.5.1 验证：`ggzyNational` 使用官方详情页返回完整样本，`ccgp` 回归正常，`cebpubservice` 保持列表可信降级；三源健康状态均为 `healthy`。

进行中：

- CEB 来源治理指标继续专项化：详情 WAF 次数、OpenClaw 深抓取成功数。

## v1.4.0

开发启动：2026-04-24
正式收口：2026-04-24
状态：已结束
目标方向：新增数据源接入能力工程化，并完成首批全国级新源试点。

已完成：

- 新增数据源接入 checklist、adapter 模板和诊断脚本。
- 新源先通过临时搜索/配置单源探测，v1.4 收口后将 `ccgp`、`ggzyNational`、`cebpubservice` 纳入生产扫描。
- 首批接入 `ccgp` 中国政府采购网，BIM 关键词本地验证可返回公开招标/磋商类公告。
- 首批接入 `ggzyNational` 全国公共资源交易平台实验 adapter；修正官方 API 参数，完成 `EPC` 非空样本字段映射。
- 根据《招投标项目信息日报任务》接入 `cebpubservice` 中国招标投标公共服务平台列表源。
- 根据日报任务文档扩展默认筛选词：应用场所、工程总承包/全过程咨询、数字孪生、管线综合、智慧运维等。
- 中国政府采购网详情页补强：从官方详情解析预算、投标/开标截止、联系人、电话、项目编号，并写入 `ccgp-detail+rules`。
- 中国招标投标公共服务平台列表源补强：保存开标时间为截止参考，并标记 `ceb-list+rules`；详情页当前触发 WAF/JS challenge，已完成 DES 解密、WAF 分类和默认关闭的详情补强开关。
- 飞书同步、重算、修复脚本来源白名单扩展到 v1.4 三个全国级来源。
- 注册新源到后端来源健康、前端筛选项和飞书来源标签。
- 修复平台筛选中广州平台展示名与入库值不一致的问题，并兼容历史别名。
- 来源健康状态结构化为：未启用、正常、空结果、请求失败、WAF 拦截、熔断中、降级可用。
- 新增来源字段质量评分、缺字段统计、脏值分类和修复建议。
- 新增 `sourceQualityTrend`，展示近 7 天 / 30 天质量分、样本数和趋势方向。
- 新增 `sourceAcceptance` 新源验收闸门，集中展示单源隔离、非空样本、字段质量、详情可靠、失败可解释、代理策略和深抓取策略。
- 新增 `sourceCandidatePool` 候选池：省级政府采购网、行业招标平台、央企/国企招采平台。
- 前端数据分析页新增“新源验收闸门”，用于判断新源是否可以加入生产扫描。

收口保留项：

- `cebpubservice` 真实 SPA 详情字段抓取不在 v1.4 中强行突破，后续需要浏览器会话/Playwright challenge cookie 或专用代理会话。
- `ggzyNational` 的 `BIM` 当前只返回成交公示，按主动招标公告过滤后为 0；`EPC` 已完成非空招标公告验证。

## v1.3.0

发布时间：2026-04-24
状态：已结束；新增数据源主线顺延到 `v1.4.0`

目标方向：后端稳定性、数据质量、广州源专项治理、AI/飞书可观测。

已完成：

- 数据治理基础：新增 `repair:tender-data` 脚本，可 dry-run / apply 修复坏链接、字段污染和历史非招采数据。
- 来源健康治理：拆分 `24h 探测失败 / 24h 轮次异常 / 最近失败原因`，前端可观测来源异常口径。
- 代理池稳定化：接入 `proxy-a`、`proxy-b`，代理池从轮询升级为健康优先 failover，坏出口失败后可自动尝试下一出口/直连。
- 广州源专项：降频、查询词分层、错误分类、低价值公告过滤、详情可信度分级、标题+正文 BIM 分类修正。
- 广州字段补强：兼容广州电子标常见截止/开标/文件获取时间写法，修复中文日期未补零导致解析失败的问题。
- AI 稳定性：OpenClaw prompt 压缩、短文本重试、并行分析、历史相关性重算并行化和规则扩词。
- AI 可观测：新增 `AiAnalysisLog`，记录 provider、状态、耗时、尝试次数、回退原因；前端 AI 面板展示成功率、回退率、平均耗时、P95 和 provider 分布。
- 详情增强：详情增强队列支持并行 worker，并在监控日志展示待处理、处理中和处理量。
- 数据分析页：新增数据质量、来源质量分布、来源治理优先级等面板。
- 飞书链路：飞书群推送和多维表同步可用，详情页新增手动推送飞书群按钮。
- 看板降噪：`ops/summary` 不再打开页面时实时探测高风险来源，改为读取最近探测和运行日志。
- OpenClaw 运维：精简 `bim-tender` agent 工作区和指令文件，归档无关历史脚本/日志/依赖。

收口保留项：

- AI 明细页暂不单独展开，只保留后端日志和汇总面板。
- 飞书完整回执时间线暂不做完整模型，保留手动推送和当前同步结果。
- 来源健康趋势图暂不扩展为长周期图表，只保留 24h 聚合和最近失败原因。
- 新增数据源不在 v1.3 主线展开，只保留 adapter/checklist 思路，正式接入放到 `v1.4.0`。

v1.4 预备方向：

- 新数据源接入 checklist 和 adapter 模板。
- 选择 1 个新数据源做试点，而不是一次性接入多个。
- 数据源接入前先完成接口探测、详情链接策略、字段模板、反爬风险和降级策略评估。

## v1.2.0

发布时间：2026-04-23
正式收口：2026-04-24
状态：已结束，最终收口标签为 `v1.2-final`

更新内容：

- 完成输入防抖搜索、命中关键词高亮，以及“仅标题搜索 / 全文字段搜索”切换
- 新增搜索建议、最近搜索记录与常用筛选视图保存
- 完成浅色主题第一轮收口，覆盖投标机会、数据分析、详情页、监控词、临时搜索主链路
- 优化投标机会清单的密度、排序、筛选与已截止显示控制
- 新增批量操作能力，支持批量标记跟进、归档、忽略
- 重做监控日志区块，补充健康信息、抓取状态、飞书启用状态与实时刷新
- 将后台扫描/刷新进度条收口到顶部 Tab 区，并压缩为轻量状态条
- 将定时抓取周期调整为每 2 小时一次
- 修复筛选下拉被 Hero 容器裁切的问题
- 修复深圳交易中心详情增强中联系人、联系电话、预算等字段缺失与脏值覆盖问题
- v1.2 收口：前端标题区版本号改为读取后端 `/api/health.version`，后端版本号从 `server/package.json` 读取，避免继续展示 `Version 1.2.0-dev`
- 详情页操作区新增“手动推送至飞书群”，用于人工把值得跟进的项目立即发送到飞书工作群
- 详情页下滑后新增圆形悬浮返回按钮，避免滚动到深处时必须回到顶部才能返回清单

## v1.1.0

发布时间：2026-04-23

更新内容：

- 将项目从泛热点监控收口为 BIM 招采监控系统
- 默认来源收敛到政府与阳光采购平台
- 前后端统一为单应用部署模式，后端直接托管前端构建产物
- 新增投标机会清单、详情页、项目时间轴、字段完整度与建议跟进等级
- 新增数据库模糊搜索能力
- 接入 Firecrawl 详情增强与二段深度解析队列
- 接入 OpenClaw / OpenRouter AI provider 路由
- 接入飞书群推送与飞书多维表格同步
- 固化广东、广州、深圳等来源的详情页 URL 构建与坏链接处理策略
